{% extends 'layoutUser.html.twig' %}

{% block title %}Attendance index{% endblock %}

{% block content %}
    <h1>Présence</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Date(s) et heure(s)</th>
            </tr>
        </thead>
        <tbody>
        {% for attendance in attendances %}
            <tr>
                <td>{{ attendance.date ? attendance.date|date('Y-m-d H:i:s') : '' }}</td>
{#                <td>#}
{#                    <a href="{{ path('attendance_show', {'id': attendance.id}) }}">show</a>#}
{#                    <a href="{{ path('attendance_edit', {'id': attendance.id}) }}">edit</a>#}
{#                </td>#}
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('attendance_new') }}"><button class="morningButton">Matin</button></a>
    <a href="{{ path('attendance_new') }}"><button class="AfternoonButton">Aprés-midi</button></a>
{% endblock %}
