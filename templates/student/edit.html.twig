{% extends 'base.html.twig' %}

{% block title %}Edit Student{% endblock %}

{% block body %}



    <div class="container">
        <h1 class="h1-modification">Apporter vos modifications</h1>
        {{ form_start(form) }}
        <a class="a-edit" href="{{ path('student_show',{'id': student.id}) }}"></a>
        {{ form_widget(form) }}

        <div class="form-group">
            <a href="{{ path('student_show', {'id': student.id}) }}">
                <button type="button" class="btn btn-signup btn-lg btn-block">Sauvegarder
                </button>
            </a>
        </div>

        <div class="form-group">
            <a href="{{ path('student_show', {'id': student.id}) }}">
                <button type="button" class="btn btn-signup btn-lg btn-block">Profil</button>
            </a>
        </div>
        {{ form_end(form) }}
    </div>

{% endblock %}
