{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-signup container">
        <h1 class="h1-signup h1 mb-3 font-weight-bold">Inscription</h1>
    </div>
    <div class="form container col-sm-offset-1 col-md-offset-3 col-xs-10 col-sm-12 col-md-6">
        {{ form_start(form) }}
        <div class="form-group">
            {{ form_row(form.email) }}
        </div>
        <div class="form-group">
            {{ form_row(form.username) }}
        </div>
        <div class="form-group">
            {{ form_row(form.plainPassword.first) }}
        </div>
        <div class="form-group">
            {{ form_row(form.plainPassword.second) }}
        </div>
        <button type="submit" class="btn btn-signup btn-lg btn-block ">Enregistrer mes informations</button>
        {{ form_end(form) }}
        <p>Vos données personnelles (email et nom d'utilisateur) ne sont utilisées qu'à des fin d'authentification et ne
            sont pas partagées avec des tiers.</p>
    </div>


{% endblock %}
