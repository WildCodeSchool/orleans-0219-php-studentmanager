{% extends 'layoutAdmin.html.twig' %}

{% block title %}Presence{% endblock %}

{% block body %}
    <h1>Presence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Date</th>
                <td>{{ presence.date ? presence.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Retards</th>
                <td>{{ retards.id }}</td>
            </tr>
            <tr>
                <th>Absences</th>
                <td>{{ absences.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('presence_index') }}">back to list</a>

    <a href="{{ path('presence_edit', {'id': presence.id}) }}">edit</a>

    {{ include('presence/_delete_form.html.twig') }}
{% endblock %}
